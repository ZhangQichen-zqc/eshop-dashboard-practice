# IT 项目管理与数据挖掘课程实验报告

> **学生**：黄兴宇（hxy5437）  
> **课程**：IT 项目管理 × 数据挖掘管理与决策  
> **项目**：Course eShop 商城经营仪表盘  
> **日期**：2026 年 6 月  
> **GitHub**：https://github.com/ZhangQichen-zqc/eshop-dashboard-practice

---

## 摘要

本项目以模拟电商商城（24 个月经营数据，20,000 用户，117,111 订单，1,036,457 行为事件）为数据基础，运用 IT 项目管理方法论完成项目规划、WBS 分解、进度控制和质量保证，运用数据挖掘方法论完成从数据探索、特征工程、分类预测、关联规则、时间序列分析到综合经营决策的完整分析链路。项目交付物为 FastAPI 仪表盘系统，支持 ETL API 数据接入，包含 11 个分析模块和 SPA 前端可视化。本报告从 IT 项目管理和数据挖掘两个维度记录项目过程，并探讨两门课程知识在实践中的交叉融合。

**关键词**：IT 项目管理；数据挖掘；WBS；RFM；复购预测；关联规则；经营决策

---

## 1. 项目背景与目标

### 1.1 项目来源

本项目是「IT 项目管理」和「数据挖掘管理与决策」两门课程的期末综合实验。教师提供了一个已运行的模拟电商系统，包含完整的后台数据库（20 张源表、14 个分析视图）和 ETL 只读接口。学生扮演数据分析师角色，需要基于商城经营数据，运用数据挖掘方法完成分析，并构建仪表盘系统供管理层查看。

### 1.2 项目目标

| 目标类型 | 具体内容 | 对应课程 |
|------|------|:--:|
| 工程目标 | 搭建 FastAPI 仪表盘，集成 11 个分析模块，支持 Docker 部署 | IT 项目管理 |
| 分析目标 | 完成数据探索、清洗、特征工程、分类、聚类、关联、时序、归因全流程 | 数据挖掘 |
| 管理目标 | 运用 WBS、里程碑、范围控制、质量保证等方法管理项目过程 | IT 项目管理 |
| 决策目标 | 基于分析结果输出经营健康度评分、增长机会、风险清单和改善建议 | 数据挖掘 |

### 1.3 项目规模

| 维度 | 数据 |
|------|------|
| 数据规模 | 20 张源表 + 14 分析视图，约 1.5M 行 |
| 代码规模 | 约 3,500 行 Python + 600 行 HTML/JS |
| 任务分解 | 23 个阶段，654 个可验证步骤 |
| 已完成 | 570 步（87.2%） |
| 协作人数 | 2 人 |
| Git 提交 | 20+ commits |

---

## 2. IT 项目管理实践

### 2.1 项目规划与 WBS 分解

#### 2.1.1 WBS 设计方法

本项目的 WBS 采用自顶向下的分解策略，最终形成 23 个阶段、654 个工作包。每个工作包均满足以下可验证标准：（1）包含明确的操作命令或验收条件；（2）可在 5-15 分钟内独立完成；（3）完成状态通过 Markdown checkbox 追踪。

WBS 的一级结构如下：

1. 阶段零：环境搭建与验证（47 步）
2. 阶段一：数据探索与理解（35 步）
3. 阶段二：ETL 数据接入层开发（82 步）
4. 阶段三：数据质量检查 R0（43 步）
5. 阶段四：特征工程（54 步）
6. 阶段五：经营驾驶舱 R1（26 步）
7. 阶段六：流量漏斗诊断 R2（27 步）
8. 阶段七：RFM 用户运营 R3（27 步）
9. 阶段八：复购预测 R4（35 步）
10. 阶段九：客户聚类 R5（25 步）
11. 阶段十：关联规则 R6（26 步）
12. 阶段十一：时间序列预测 R7（41 步）
13. 阶段十二：营销归因 R8（26 步）
14. 阶段十三：履约售后 R9（20 步）
15. 阶段十四：库存策略 R10（15 步）
16. 阶段十五：综合决策 R11（22 步）
17. 阶段十六至二十三：前端、Docker、测试、文档、答辩（84 步）

#### 2.1.2 WBS 与数据挖掘流程的对应关系

WBS 的阶段顺序并非随意排列，而是与数据挖掘的标准流程（CRISP-DM）严格对应。这一设计确保项目推进的逻辑一致性：

| CRISP-DM 阶段 | WBS 对应阶段 | 依赖关系说明 |
|------|:--:|------|
| 业务理解 | 阶段零、一 | 必须先理解数据和指标口径，才能开展后续分析 |
| 数据理解 | 阶段一 | 遍历 34 张表是特征工程的前提 |
| 数据准备 | 阶段三、四 | 数据清洗（R0）→ 特征工程（阶段四），顺序不可逆 |
| 建模 | 阶段五至十二 | 各模块依赖特征工程的产出（用户宽表） |
| 评估 | 各模块内部 | 每个算法问题均要求多模型对比和指标量化 |
| 部署 | 阶段二、十六、二十 | FastAPI 化 → 前端可视化 → Docker 容器化 |

#### 2.1.3 个人实践体会

WBS 分解的粒度直接影响后续开发的效率。以阶段四「特征工程」为例，任务清单将 RFM 拆解为 6 个独立步骤（计算基础 RFM → Recency → Frequency → Monetary → 评分 → 分群标签），每一步都有明确的输入和输出。这种粒度在实际编码中避免了「一次性写完几百行然后调试半天」的问题——每完成一步即可运行验证，发现问题立即修正。这体现了项目管理中「工作包可独立交付」原则在软件开发场景下的适配：对于数据挖掘任务，工作包的粒度应与「可独立计算的指标」对齐。

### 2.2 进度管理

#### 2.2.1 里程碑计划

| 里程碑 | 计划完成 | 实际完成 | 偏差分析 |
|------|------|------|------|
| M0 环境就绪 | Week 1 初 | Week 1 初 | 按计划完成 |
| M1 数据理解 | Week 1 中 | Week 1 中 | 按计划完成 |
| M2 架构搭建 | Week 1 末 | Week 1 末 | 按计划完成 |
| M3 核心分析（R0-R11） | Week 2-3 | Week 2 完成 | 提前约 1 周 |
| M4 前端交付 | Week 3 | Week 3 初 | 提前完成 |
| M5 ETL 整合 | Week 4 | Week 4 初 | 按计划完成 |
| M6 Docker 部署 | Week 4 | 待完成 | — |
| M7 最终交付 | Week 4 | 待完成 | — |

核心分析阶段提前一周完成，主要原因是：（1）`data_access.py` 提供的统一数据接口使各子项目无需重复编写数据连接代码；（2）独立的分析模块支持并行开发；（3）AI 辅助编程（Claude Code）显著加快了代码生成和调试速度。

#### 2.2.2 进度追踪机制

项目使用 `my_task.md` 作为统一任务看板，每完成一步即将 `- [ ]` 改为 `- [x]`。每完成一个阶段执行 `git commit` 并推送到 GitHub，远端始终为最新状态。这种机制的优势在于：任务清单同时承担了「计划文档」和「进度报告」两个角色，减少了项目管理开销。

#### 2.2.3 个人实践体会

进度管理中最有价值的经验是「正反馈循环」的建立。654 步的清单初看令人望而生畏，但每打一个钩就获得一次微小的完成感，这种节奏推动着项目持续前进。与之对比，如果没有这样细粒度的任务分解，很容易在完成一个大模块后陷入「不知道下一步做什么」的停滞状态。这在项目管理理论中被称为「进展可视性原则」——在软件开发场景下，将抽象的功能需求转化为具体的、可勾选的任务项，是维持开发者动力的有效手段。

### 2.3 范围管理

#### 2.3.1 优先级规划

基于教师的最低交付要求，将 13 个分析模块分为三个优先级：

| 优先级 | 模块 | 依据 |
|:--:|------|------|
| P0 | 数据质量 R0、驾驶舱 R1、RFM R3、复购预测 R4、销售预测 R7、营销归因 R8、决策中心 R11 | 教师要求的最低交付 |
| P1 | 漏斗 R2、聚类 R5、关联 R6、履约 R9、库存 R10 | 完整版加分项 |
| P2 | AI 助手、高级扩展功能 | 选做 |

实际执行中，P0 和 P1 全部完成。P2 因时间限制未启动，但系统架构预留了扩展接口（如 `subprojects/` 目录可直接添加新模块）。

#### 2.3.2 范围变更记录

| 变更 | 触发条件 | 影响范围 | 决策过程 |
|------|------|------|------|
| ETL 严格模式 | 教师明确要求数据必须通过 ETL 接口获取 | `data_access.py`（~200 行） | 评估确认子项目无需修改后实施 |
| 前端 SPA 化 | 原多页面方案用户体验不佳 | `index.html`（~600 行重写） | 确认后端 API 完全兼容后实施 |
| 分工文档 | 教师要求两人独立报告 | 新增 `attribution.md` | 无代码影响，即时实施 |

#### 2.3.3 个人实践体会

范围管理中最大的教训来自 ETL 重构。项目初期为快速出成果，所有数据访问直接使用 SQLite——这在当时是正确的决策（快速验证分析逻辑），但后期范围变更（必须走 ETL）迫使回改。如果初期就将数据源抽象为接口（而非直接依赖 SQLite 连接），这次变更的代价会小得多。这验证了软件工程中「依赖倒置原则」的实践价值：高层模块不应依赖低层模块，二者都应依赖抽象。在数据挖掘项目中，「数据源」就是那个需要被抽象的底层模块。

### 2.4 质量管理

#### 2.4.1 质量体系设计

本项目从四个层次定义质量管理：

| 层次 | 度量标准 | 检查手段 | 执行结果 |
|------|------|------|------|
| 代码质量 | docstring 覆盖率、日志规范性、函数单一职责 | 人工 Review | 所有公开函数均有 docstring |
| 数据质量 | 完整性/唯一性/准确性/时效性/一致性 | R0 模块 36 项自动检查 | 35/36 通过 |
| 模型质量 | AUC/Precision/Recall/F1/MAPE/轮廓系数 | 交叉验证、多模型对比 | 详见第 3 章 |
| 系统质量 | API 响应正确性、前端页面可访问性 | 端到端测试 | 50+ 端点全部正常 |

#### 2.4.2 数据质量检查（R0 模块）

R0 模块执行 6 大维度 36 项自动检查，结果如下：

| 维度 | 项数 | 通过 | 关键发现 |
|------|:--:|:--:|------|
| 完整性 | 16 | 16 | 用户关键字段无缺失，orders 金额字段完整 |
| 唯一性 | 4 | 4 | users/orders/sku/campaigns 主键无重复 |
| 准确性 | 6 | 6 | paid_amount 无负数，price ≥ cost 约束成立 |
| 时效性 | 4 | 4 | 数据覆盖 2024-04-02 ~ 2026-03-31（24 个月） |
| 一致性 | 3 | 3 | 外键无孤立记录 |
| 业务逻辑 | 3 | 2 | 订单状态分布合理；1 项因 payments 表字段名差异跳过 |

35/36 通过的数据质量为后续所有分析提供了可信度基础。这也是项目管理中「质量保证前置」的体现：与其在建模完成后怀疑数据有问题，不如在流程起点就系统性地验证数据。

#### 2.4.3 模型质量保证

每个算法问题均采用多模型对比策略，避免单一模型的偶然性：

- 复购预测（R4）：逻辑回归、决策树、随机森林、梯度提升 4 模型对比，以 AUC 为选型标准
- 时间序列（R7）：朴素预测、移动平均、指数平滑、ARIMA 4 模型对比，以 MAPE 为选型标准
- 客户聚类（R5）：K-Means、层次聚类、DBSCAN 3 算法对比，以轮廓系数为选型标准

#### 2.4.4 个人实践体会

质量检查中最有启发性的是关联规则分析（R6）的结果：所有 66 个品类 pair 的 lift 值均 < 1.0，即不存在强关联。从模型评估角度看，这不是「模型失败」——它是真实的数据特征。但如果只关注技术指标而忽略业务解读，很容易强行调低支持度阈值来「凑出」几条规则。这体现了质量管理的一个重要维度：**业务合理性验证**。技术指标（lift > 1.2）和业务常识（12 个独立品类确实不应有强关联）需要相互印证，任何一方的缺失都可能导致错误结论。

### 2.5 配置管理与版本控制

#### 2.5.1 Git 工作流

项目采用简化版 Feature Branch 工作流，直接在 main 分支上按阶段线性提交：

```
309c4b8 → 16a50b4 (zhangqichen: 初始化 + 任务清单)
25ebe9c → 83effe7 → a327b63 → 734aa72 → 1b26131 →
f72512e → 6f828b1 → 51ca719 → f4917c0 → a62e282 →
a8018aa → a274a4f → efea208 → 850f2df → e3d4f98 →
c166bc5 → f956e23 → 21806ea → 4484843 (hxy5437: 阶段零至十六 + ETL)
```

#### 2.5.2 提交规范

| 规范项 | 内容 |
|------|------|
| 粒度 | 每完成一个阶段提交一次 |
| 格式 | `完成阶段XX：模块名称 + 关键成果列表` |
| 签名 | 两人使用独立 Git 身份（hxy5437 / zhangqichen），贡献可追溯 |
| 协作者 | AI 辅助标注 `Co-Authored-By: Claude <noreply@anthropic.com>` |

#### 2.5.3 环境可复现性

| 层面 | 措施 |
|------|------|
| Python 依赖 | `requirements.txt` 锁定 13 个包及版本 |
| Node 依赖 | `package.json` 管理 server/client 依赖 |
| 虚拟环境 | `venv/` 隔离，`.gitignore` 排除 |
| 运行模式 | `DATA_SOURCE_MODE` 环境变量控制 etl/sqlite 切换 |
| Docker | `docker-compose.yml` 目标为 Ubuntu Linux |

#### 2.5.4 个人实践体会

版本控制的价值在本项目中体现在两个时刻。第一，ETL 重构时修改了 `data_access.py` 约 200 行，如果不使用 Git，一旦改出问题将难以回溯。第二，教师要求的「`git log --oneline --graph` 能体现功能演进」本质上是在考察**增量交付能力**——不是一次性堆出所有功能，而是每一步都有可验证的产出。这与项目管理中的迭代开发理念一致：每个阶段交付一个可用的增量，而非等到最后才展示结果。

### 2.6 风险管理

| 风险 | 概率 | 影响 | 应对措施 | 实际触发情况 |
|------|:--:|:--:|------|------|
| ETL API 不可用 | 中 | 高 | 启动时强制检查，不可用则拒绝启动 | 首次启动时触发，启动商城 API 后解决 |
| 数据源与 SQLite 不一致 | 低 | 高 | 统一通过 data_access 层访问 | 未触发 |
| 模型训练时间过长 | 中 | 中 | 向量化替代逐行循环 | 时间窗口特征加速 10x+ |
| 数据泄漏 | 中 | 高 | 代码 Review + 时间切分验证 | 复购预测中被及时发现并修正 |
| 提交冲突 | 低 | 低 | 按模块分文件，各自负责独立文件 | 未触发 |

### 2.7 协作与沟通

| 沟通活动 | 工具/方式 | 产出 |
|------|------|------|
| 任务分配 | my_task.md | 654 步清单，按讲义章节分工 |
| 代码同步 | Git commit + push | 20+ 提交，远端始终最新 |
| 方案讨论 | VS Code 终端 + 实时交流 | ETL 重构方案、分工调整 |
| 进度更新 | my_task.md checkbox | 实时完成度可见 |

### 2.8 项目管理反思

回顾整个项目周期，以下几点对项目成功起到了关键作用：

1. **细粒度 WBS 是自驱力的来源**。654 步的清单将 4 周的工作分解为每天可完成的小任务，避免了面对大型项目时的启动困难。
2. **数据源抽象层是范围弹性的保障**。`data_access.py` 的统一接口使 ETL 重构仅需修改一个文件，11 个子项目零改动。这验证了架构设计对项目范围变更的缓冲作用。
3. **质量保证前置优于事后修复**。R0 模块在项目早期就完成了全量数据检查，避免了后期分析中发现数据问题时的大规模回退。
4. **工具链一致性不可忽视**。Node.js 在 Windows/WSL 之间的路径差异（UNC 路径问题）曾导致依赖安装失败，这提醒了环境标准化的重要性。

---

## 3. 数据挖掘管理与决策实践

### 3.1 方法论框架

本项目遵循 CRISP-DM（跨行业数据挖掘标准流程）方法论，各阶段在本项目中的映射如下：

| CRISP-DM 阶段 | 项目对应 | 关键产出 |
|------|------|------|
| 业务理解 | 指标口径定义、初步业务发现 | 8 大指标定义、500 字业务报告 |
| 数据理解 | 阶段一：数据探索 | 34 张表遍历、分布分析 |
| 数据准备 | R0 数据质量 + 阶段四特征工程 | 6 维质量检查、63 维用户宽表 |
| 建模 | R4-R8 | 分类/聚类/关联/时序/归因 5 类模型 |
| 评估 | 多模型对比、交叉验证 | AUC/MAPE/Silhouette 量化对比 |
| 部署 | FastAPI + 前端 + Docker | 50+ API 端点、SPA 仪表盘 |

### 3.2 数据理解与探索

#### 3.2.1 数据资产

项目涉及 20 张源表、14 个分析视图、1 个汇总视图，数据规模约 150 万行：

| 表类型 | 代表表名 | 行数 | 用途 |
|------|------|------:|------|
| 用户维度 | dim_user | 20,000 | 用户属性 |
| 商品维度 | dim_product | 864 | 商品/品类/品牌 |
| 日期维度 | dim_date | 749 | 日期属性（周末/月份） |
| 活动维度 | dim_campaign | 56 | 营销活动信息 |
| 订单事实 | fact_order | 117,111 | 订单主记录 |
| 流量事实 | fact_traffic | 1,036,457 | 用户行为事件 |
| 订单行事实 | fact_order_item | 247,955 | 订单明细 |
| 履约事实 | fact_fulfillment | 117,111 | 配送记录 |
| 汇总视图 | daily_business_summary | 4,153 | 经营日报 |

#### 3.2.2 核心指标口径

| 指标 | 公式 | 计算值 |
|------|------|------:|
| GMV | `SUM(paid_amount) WHERE status IN ('paid','completed')` | ¥156,024,870 |
| 净销售额 | GMV - 已批准退款 | ¥145,715,684 |
| 毛利 | `SUM((unit_price - unit_cost) × quantity - discount)` | ¥79,232,068 |
| 毛利率 | 毛利 / GMV | 50.78% |
| 订单数 | `COUNT(DISTINCT order_id)` paid/completed | 108,040 |
| 客单价 | GMV / 订单数 | ¥1,444 |
| 退款率 | 退款订单 / 总订单 | 7.75% |
| 转化率 | pay_success session / 全部 session | 49.39% |

#### 3.2.3 初步业务发现

数据时间跨度为 2024-04 至 2026-03（24 个月）。月度 GMV 呈现三段式走势：2024 Q2-Q3 起步期（月均 200-400 万）、2024 Q4-2025 Q2 爆发期（峰值约 1,100 万）、2025 Q3-Q1 稳定期（月均 500-700 万）。organic 渠道贡献最高（26.8%），数码电子品类 GMV 第一（3,580 万，占 23%）。毛利率 50.8% 显著高于电商行业平均（20-40%），属于高质量经营。

### 3.3 特征工程

#### 3.3.1 用户宽表设计

从 dim_user 基础属性出发，融合 RFM、行为、时间窗口、优惠券、品类偏好、购物行为六大维度，构建了 20,000 行 × 63 列的用户宽表：

| 特征维度 | 特征数 | 代表性特征 | 构造方法 |
|------|:--:|------|------|
| 用户属性 | 11 | user_id, member_level, province | 直接从 dim_user 继承 |
| RFM | 9 | recency, frequency, monetary, R_score, rfm_segment | 订单聚合 + 分位数评分 |
| 行为特征 | 7 | total_events, active_days, search_count | traffic 表 pivot_table |
| 时间窗口 | 12 | events_7d/30d/90d, cart_7d/30d/90d | 向量化 groupby + merge |
| 优惠券 | 7 | coupons_issued, coupon_use_rate, days_since_last_coupon | coupon_use 表聚合 |
| 品类偏好 | 5 | top_category, category_diversity, high_price_ratio | order_items 关联 products |
| 购物行为 | 8 | monthly_orders, lifecycle_days, weekend_order_ratio | orders 聚合 + 时间计算 |

#### 3.3.2 数据泄漏防护

在构造复购预测特征时，严格遵守特征窗口与标签窗口隔离原则。参考日期设为 2025-12-31——所有特征仅使用参考日期之前的历史数据（观察窗口 365 天），标签定义为参考日期之后 60 天内是否有购买。这一防护措施避免了在模型训练中引入未来信息。

#### 3.3.3 个人实践体会

特征工程中最容易犯的错误是「无差别地构造特征」，即把能算出来的特征全部扔进宽表，而不考虑每个特征的业务含义。本项目中有意识地按照 RFM → 行为 → 品类偏好的层次递进构造特征，每个维度回答一个特定的业务问题（RFM 回答「用户价值」、行为特征回答「用户活跃度」、品类偏好回答「用户消费倾向」）。这种结构化思维来源于数据挖掘课程中对特征类型的分类训练，但只有在实际编码中才能体会到：好的特征分组不仅便于调试（出问题时知道查哪个维度），也为后续的模型解释提供了框架（特征重要性可以按维度汇总）。

### 3.4 复购预测模型（R4）

#### 3.4.1 问题定义

- **任务类型**：二分类
- **目标变量**：`will_repurchase_in_60d`（参考日期后 60 天内是否有购买）
- **参考日期**：2025-12-31
- **样本量**：14,842 人（正例 8,310，负例 6,532，比例 1.27:1）

#### 3.4.2 模型对比

| 模型 | AUC | Precision | Recall | F1 | 训练时间 |
|------|------:|------:|------:|------:|:--:|
| 逻辑回归 | 0.9554 | 0.8711 | 0.8813 | 0.8762 | 快 |
| 决策树 | 0.9941 | 0.9602 | 0.9972 | 0.9784 | 快 |
| 随机森林 | 0.9959 | 0.9609 | 0.9948 | 0.9775 | 中 |
| **梯度提升** | **0.9977** | **0.9707** | **0.9952** | **0.9828** | 慢 |

梯度提升在四项指标上均最优，选为最终模型。

#### 3.4.3 特征重要性分析

| 排名 | 特征 | 重要性 | 业务解读 |
|:--:|------|------:|------|
| 1 | recency | 0.129 | 最近购买时间是复购意愿的最强信号 |
| 2 | events_90d | 0.121 | 90 天行为活跃度反映用户粘性 |
| 3 | cart_90d | 0.091 | 近期加购行为预示购买意图 |
| 4 | first_order_days_ago | 0.074 | 新老用户复购行为差异显著 |
| 5 | lifecycle_days | 0.069 | 用户成熟度影响复购概率 |

#### 3.4.4 模型落地：ROI 计算

基于模型预测概率排序，Top 5% 用户触达分析：

| 参数 | 数值 |
|------|------:|
| 触达人数 | 1,053 |
| 假设触达成本 | ¥5/人 |
| 总触达成本 | ¥5,265 |
| 预期复购人数 | 1,050 |
| 预期收入（客单价×复购人数） | ¥1,516,200 |
| **ROI** | **287x** |

#### 3.4.5 个人实践体会

梯度提升的 AUC 达到 0.9977 时，最初的反应是「是不是过拟合了」。检查后发现两点：（1）训练集和测试集的 AUC 差距很小（约 0.002），排除了严重过拟合；（2）recency 以 0.129 的权重位居第一重要性特征，在业务逻辑上完全合理——你最近有没有买过东西，本来就是复购意愿最直接的信号。这提醒我：模型评估不能只看数字，特征重要性的业务合理性是另一个重要的校验维度。这也是数据挖掘课反复强调的「可解释性」——对商业场景而言，一个 AUC 0.95 但能逐条解释的模型，可能比一个 AUC 0.99 但完全黑盒的模型更有用。

### 3.5 关联规则分析（R6）

#### 3.5.1 购物篮构建

从 `fact_order_item` 构建三种粒度的购物篮：

| 粒度 | 规模 | 分析方法 |
|------|------|------|
| SKU 级 | 80,399 订单 × TOP 80 SKU | Apriori（min_support=0.0005） |
| 类目级 | 76,634 订单 × 12 类目 | 直接计算 co-occurrence lift |
| 价格带级 | 按 low/mid/high | Apriori |

#### 3.5.2 分析结果

类目级共 50 个频繁项集（单品类 + 品类 pair）。然而，所有 66 个品类 pair 的 lift 值均 < 1.0（最高为 服饰鞋包+美妆个护，lift=0.78）。

#### 3.5.3 业务解读

Lift < 1.0 的全局性结论表明，12 个品类之间的购买行为是相互独立的——用户购买数码电子并不会提高其购买美妆个护的概率，反之亦然。这一发现与传统的「买了 A 也买 B」关联推荐逻辑相悖，但数据本身是可信的（样本量足够大、品类覆盖完整）。

#### 3.5.4 决策转化

基于此发现，建议放弃强制性捆绑销售策略，转向两个方向：（1）基于用户行为协同过滤的个性化推荐；（2）品类内而非跨品类的搭配推荐。这是数据挖掘中「负面结果也有决策价值」的典型案例。

#### 3.5.5 个人实践体会

关联规则是唯一一个「跑出来没结果」的模块。最初尝试 SKU 级 Apriori 时遭遇了内存爆炸（864 SKU × 80K 订单的 one-hot 矩阵需要 54.5 GB），被迫限制到 TOP 80 SKU，结果仍然没有强规则。转换为类目级分析后，发现 lift 全部低于 1.0。这个过程中学到了一件事：**数据挖掘不是算法调参比赛，有时候数据本身就告诉你「这条路走不通」**。强行降低支持度阈值来凑规则在技术上是可行的，但在业务上是自欺欺人的。如实报告「类目间无强关联，建议转向其他推荐策略」，比展示几条 lift=1.1 的弱规则更有价值。

### 3.6 时间序列预测（R7）

#### 3.6.1 序列构建与平稳性检验

构建 729 天日 GMV 序列（2024-04-02 ~ 2026-03-31），缺失日期用 0 填充。ADF 检验 p > 0.05，序列非平稳，需要 1 阶差分后达到平稳。

#### 3.6.2 模型对比

| 模型 | MAPE | 适用场景 | 优缺点 |
|------|------:|------|------|
| 朴素预测 | 7.0% | 基线 | 过于简单，无趋势捕捉 |
| 移动平均 | 7.0% | 短期平稳 | 滞后性强 |
| **指数平滑** | **6.3%** | 近期数据权重大 | 🏆 最优，适合短期预测 |
| ARIMA(1,1,1) | 10.7% | 线性趋势 | 过拟合，泛化差 |

指数平滑以 MAPE 6.3% 为最优模型。ARIMA 的糟糕表现可能源于该序列的非线性特征——电商 GMV 受促销活动、季节性等多因素干扰，ARIMA 的线性假设难以捕捉这些模式。

#### 3.6.3 安全库存计算

基于 TOP 20 SKU 的日销量统计，采用 95% 服务水平（Z=1.65）计算安全库存：

`安全库存 = Z × σ_daily × √(lead_time)`（假设 lead_time = 3 天）

| 参数 | 范围 |
|------|------|
| 日均需求 | 3.5 - 5.6 件 |
| 需求变异系数 CV | 0.80 - 0.92 |
| 安全库存建议 | 8 - 13 件 |

### 3.7 综合决策中心（R11）

#### 3.7.1 健康度评分模型

构建 7 维度加权评分模型（满分 100）：

| 维度 | 权重 | 得分 | 得分率 |
|------|:--:|------:|:--:|
| GMV 趋势 | 20 | 20.0 | 100% |
| 毛利率 | 20 | 20.0 | 100% |
| 转化率 | 15 | 14.8 | 99% |
| 退款率 | 15 | 3.4 | 23% |
| 客户活跃度 | 10 | 3.2 | 32% |
| 配送时效 | 10 | 0.0 | 0% |
| 库存周转 | 10 | 0.0 | 0% |
| **总计** | **100** | **61.4** | **C 级** |

评分暴露的核心问题是配送和库存两个维度的严重短板。改善路径清晰：优先优化配送时效（降低延迟率），其次改善库存周转，预计可将健康度提升至 B 级（75+）。

#### 3.7.2 TOP 3 增长机会与风险

| 类型 | 内容 | 数据支撑 | 优先级 |
|------|------|------|:--:|
| 机会 | 加大直播投入 | 直播转化率 53.75%（全渠道最高），GMV 仅占 4.9% | P0 |
| 机会 | 高价值客户召回 | 核心价值客户 1,924 人贡献 25.4% GMV，流失客户 7,178 人待激活 | P0 |
| 机会 | 配送优化减少退款 | 延迟订单退款率 10.32% vs 准时 4.76%（+5.56pp） | P1 |
| 风险 | GMV 中期回落 | 2025 H2 GMV 从峰值 1,100 万/月回落至 500-700 万 | 高 |
| 风险 | 直播 ROAS 偏低 | 1.94x，远低于 search 9.36x | 中 |
| 风险 | 差评率偏高 | 15.5%，影响复购 | 中 |

#### 3.7.3 个人实践体会

健康度评分的设计过程体现了数据挖掘中「权重确定」的困境：GMV 趋势和毛利率各占 20 分，这个权重是主观设定的，如果调整权重分配，总分可能从 C 变成 B。为了解决这个问题，我在报告中明确标注了每个维度的得分和权重，让读者（老师）可以自行判断权重的合理性。这种透明性本身就是一种严谨性——承认主观判断的存在，而不是用复杂的数学包装出「客观」的假象。

### 3.8 数据挖掘反思

1. **CRISP-DM 流程的实用性**。严格按照「业务理解 → 数据理解 → 数据准备 → 建模 → 评估 → 部署」的流程推进，避免了跳过数据探索直接建模的常见错误。
2. **数据质量是分析质量的基石**。R0 模块的 36 项检查虽然在流程中占时间，但后续所有模型都受益于对数据质量的信心。
3. **多模型对比比单一模型更有说服力**。每个算法问题都至少对比 3 种模型，既展示了方法的选择空间，也提供了模型性能的上限参考。
4. **模型评估指标的选择要与业务场景匹配**。复购预测关注 Precision（触达名单的准确性），时间序列关注 MAPE（预测偏差的直观理解），聚类关注轮廓系数（簇内紧密度）——不同任务适用不同指标，不应一刀切地用准确率。

---

## 4. 系统架构与工程实践

### 4.1 架构设计

系统采用三层架构：数据层（data_access.py）→ 业务层（subprojects/）→ 表现层（main.py + static/index.html）。

```
用户浏览器 (:8000)
    │
FastAPI 仪表盘
    ├── main.py (路由分发，50+ 端点)
    ├── data_access.py (ETL 严格模式 + SQLite 计算引擎)
    └── subprojects/ (12 个分析模块)
    │
商城 API (:38173)
    └── /api/etl/* (只读数据接口)
    │
SQLite (eshop.sqlite, 262 MB)
```

### 4.2 ETL 严格模式

根据教学要求，实现了 ETL 优先的严格数据源模式：
- 模块加载时强制检查 ETL API 可用性，不可用则抛出 `ConnectionError` 并提示启动商城
- 所有 `load_*` 函数通过 `ETLClient.fetch_all()` 从 API 分批拉取数据
- `get_db_connection()` 保留为复杂分析查询（JOIN/聚合）的计算引擎
- 支持通过 `DATA_SOURCE_MODE=sqlite` 环境变量切换到直连模式

### 4.3 前端设计

SPA 单页应用，12 个模块页面通过 JavaScript 路由切换，无需刷新。Chart.js 渲染 8 种图表类型（柱状图、折线图、饼图、环形图、雷达图）。响应式布局支持桌面和移动端。

---

## 5. 两门课程知识融合分析

本项目最核心的学习收获在于两门课程知识的交叉应用。以下是几个具体的融合点：

### 5.1 WBS 分解依赖于算法理解

WBS 中的工作包粒度需要与数据挖掘任务的计算单元对齐。例如，RFM 特征工程被分解为 6 个独立步骤（计算基础值 → 三个分指标 → 分位数评分 → 分群标签），而非一个「实现 RFM」的大任务。这种粒度要求分解者理解每个指标的计算依赖关系：Recency、Frequency、Monetary 是独立可并行的，但分群标签依赖所有三个分值。**只有理解了算法原理，才能做出合理的工作包分解**。

### 5.2 质量保证贯穿数据挖掘全流程

项目管理中的质量保证理念在数据挖掘中体现为三个检查点：输入质量（R0 数据检查）→ 过程质量（特征工程中避免数据泄漏）→ 输出质量（多模型对比评估）。任何一个检查点的缺失都可能导致最终结论的偏差。项目初期执行的全量数据质量检查为后续所有模型提供了可信度背书，这验证了「质量保证前置」的效益。

### 5.3 范围变更受架构设计约束

ETL 重构是项目中最重要的一次范围变更。由于初期将数据访问逻辑全部收敛在 `data_access.py` 一个文件中，这次变更仅修改了约 200 行代码，11 个子项目零改动。如果初期没有采用分层架构，每个子项目直接写 SQLite 连接代码，变更成本将是现在的 10 倍以上。**架构设计决定了项目对范围变更的适应能力**，这是软件工程原则在数据挖掘项目中的直接应用。

### 5.4 决策输出需要管理框架

数据挖掘产出的是分析结果（指标、模型、规则），但只有将这些结果放入管理决策框架（优先级排序、ROI 估算、风险评估），才能完成从「数据分析师」到「业务决策支持者」的角色转变。R11 综合决策中心的设计正是这一融合的体现：健康度评分提供了一个全局视角（管理），而各维度的得分来源于 R1-R10 的分析结果（数据）。

---

## 6. 总结

### 6.1 项目完成情况

| 维度 | 完成度 | 说明 |
|------|:--:|------|
| 环境搭建 | 100% | Python/Node/Docker/Git 全部就绪 |
| 数据探索 | 100% | 34 张表遍历、指标口径文档 |
| ETL 接入 | 100% | 严格 ETL 模式，API 降级链 |
| 数据挖掘模块（P0） | 100% | R0/R1/R3/R4/R7/R8/R11 全部完成 |
| 数据挖掘模块（P1） | 100% | R2/R5/R6/R9/R10 全部完成 |
| 仪表盘前端 | 100% | SPA 12 模块，50+ API 端点 |
| Docker 部署 | 待完成 | docker-compose.yml 编写中 |
| 测试与文档 | 待完成 | 冒烟测试、答辩 PPT 编写中 |

### 6.2 关键学习收获

1. **项目管理方法论在小型项目中依然有效**。654 步 WBS 看似过度管理，实际上通过正反馈循环维持了 4 周的高效率推进。
2. **数据源抽象是数据挖掘工程化的核心**。将数据访问与业务逻辑分离，使系统具备了对需求变更的适应能力。
3. **模型评估需要技术和业务双重维度**。AUC 高不等于模型好，特征重要性的业务合理性、lift 的业务可解释性同样重要。
4. **负面分析结果同样具有决策价值**。关联规则分析得出「无强关联」的结论，回答了「不应该做什么」的问题。
5. **两门课程不是加法关系，而是乘法关系**。IT 项目管理提供了完成任务的框架，数据挖掘提供了完成任务的方法——两者在实践中互相检查、互相深化。

---

## 附录

### A. 项目文件结构

```
eshop-dashboard-practice/
├── analytics_dashboard/app/
│   ├── main.py                   ← FastAPI 入口
│   ├── config.py                 ← 配置（含 DATA_SOURCE_MODE）
│   ├── utils.py                  ← 15 个工具函数
│   ├── data_access.py            ← ETL 严格模式数据层
│   └── subprojects/              ← 12 个分析模块
│       ├── business_health.py    ← R1 经营驾驶舱
│       ├── traffic_funnel.py     ← R2 流量漏斗
│       ├── rfm_user_ops.py       ← R3 RFM 用户运营
│       ├── feature_engineering.py← 用户/商品宽表
│       ├── repurchase_prediction.py ← R4 复购预测
│       ├── customer_clustering.py   ← R5 客户聚类
│       ├── association_rules.py     ← R6 关联规则
│       ├── sales_forecast.py        ← R7 时间序列
│       ├── marketing_attribution.py ← R8 营销归因
│       ├── fulfillment_analysis.py  ← R9 履约售后
│       ├── inventory_strategy.py    ← R10 库存策略
│       └── decision_board.py        ← R11 综合决策
├── static/index.html             ← SPA 前端
├── notebooks/                    ← 数据探索
├── docs/                         ← 指标口径、数据清单、业务发现
├── my_task.md                    ← 654 步任务清单
├── attribution.md                ← 分工记录
└── hxy.md                        ← 本报告
```

### B. Git 提交历史（摘要）

```
4484843  重构数据源架构：ETL API 优先，SQLite 降级
21806ea  完成阶段十六：仪表盘前端整体完善
f956e23  完成阶段十五：综合决策中心 R11
c166bc5  完成阶段十四：库存策略 R10
e3d4f98  完成阶段十三：履约与售后 R9
850f2df  完成阶段十二：营销归因 R8
efea208  完成阶段十一：时间序列预测 R7
a274a4f  完成阶段十：关联规则 R6
a8018aa  完成阶段九：客户聚类 R5
a62e282  完成阶段八：复购预测模型 R4
f4917c0  完成阶段七：RFM 用户运营 R3
51ca719  完成阶段六：流量漏斗诊断 R2
6f828b1  完成阶段五：经营驾驶舱 R1
f72512e  完成阶段四：特征工程
1b26131  完成阶段三：数据质量检查 R0
734aa72  完成阶段二：ETL 数据接入层 + FastAPI 骨架
a327b63  完成阶段一：数据探索与理解
25ebe9c  完成阶段零：环境搭建与验证
16a50b4  初始任务清单（zhangqichen）
309c4b8  项目初始化（zhangqichen）
```

---

*报告结束。项目代码见 https://github.com/ZhangQichen-zqc/eshop-dashboard-practice*
